Import('base_env SetupNSPR SetupSDL')

SOURCES = [
    'ListenerThread.cpp',
    'Main.cpp',
    'World.cpp']

env = base_env.Copy()

SetupNSPR(env)
env.Append(LIBS = ['base'])

env.Program('#/build/server', SOURCES)
