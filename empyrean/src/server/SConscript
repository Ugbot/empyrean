Import('base_env TOOLS')

SOURCES = [
    'ListenerThread.cpp',
    'Main.cpp',
    'World.cpp']

env = base_env.Copy()
for t in Split('PyrBase NSPR'):
    TOOLS[t](env)

env.Program('#/build/server', SOURCES)
